# SkillSync Database

## Project Description
A database for connecting ALU students with local gig opportunities in Kigali.

## Database Setup
1. Run `schema.sql` to create the database structure
2. Run `seed.sql` to add sample data
3. Use `queries.sql` for common operations

## Tables
- users, students, providers, gigs, applications, transactions, reviews